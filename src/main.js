// The webview opens directly on https://web.whatsapp.com, so this file is
// only present to satisfy Tauri's `frontendDist` requirement. The real
// frontend integration (notification bridge) lives in the Rust-side
// `INJECTION_SCRIPT`, which is injected before every page load.
